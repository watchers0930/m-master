# m-master 구현 규칙

이 문서는 `m-master` 구현 시 반드시 따를 코드 레벨 기준이다.

## 1. 기본 원칙

- 구현은 항상 `명확성`, `분리`, `검증 가능성`을 우선한다.
- 한 파일이 너무 많은 책임을 가지지 않게 한다.
- 임시 코드가 제품 구조가 되지 않게 한다.
- 짧게 쓰는 것보다 읽기 쉽게 쓰는 것을 우선한다.
- 새 기능은 제거와 수정이 쉬운 방향으로 만든다.

## 2. 폴더 구조

- `app/`은 라우팅과 페이지 조합 책임만 가진다.
- `components/`는 재사용 UI 컴포넌트를 둔다.
- `features/`는 도메인 단위 UI, 상태, 액션을 둔다.
- `lib/`는 공통 유틸과 얇은 클라이언트를 둔다.
- `server/`는 서버 전용 비즈니스 로직, 서비스, 리포지토리를 둔다.
- `prisma/`는 DB 스키마와 마이그레이션만 관리한다.
- `types/`는 공용 타입을 둔다.
- 같은 책임의 코드가 여러 폴더에 흩어지지 않게 한다.

## 3. 페이지와 라우트

- 페이지 파일은 데이터 연결과 화면 조합만 담당한다.
- 페이지에서 직접 복잡한 비즈니스 로직을 작성하지 않는다.
- API Route Handler는 얇게 유지한다.
- 요청 파싱, 권한 확인, 서비스 호출, 응답 반환 순서를 명확히 지킨다.
- Route Handler 안에 Prisma 쿼리를 직접 길게 작성하지 않는다.

## 4. 컴포넌트

- 반복되는 UI와 기능은 반드시 컴포넌트로 분리한다.
- 프레젠테이션 컴포넌트와 비즈니스 로직 컴포넌트를 분리한다.
- 하나의 컴포넌트는 하나의 명확한 역할만 가진다.
- props가 과도하게 많아지면 컴포넌트 분리를 먼저 검토한다.
- UI 컴포넌트에서 DB, API, 비즈니스 규칙을 직접 다루지 않는다.
- 공통 버튼, 입력, 모달, 패널, 배지, 카드 패턴은 재사용 가능하게 만든다.

## 5. 기능 단위 구성

- 하나의 기능은 가능하면 `UI`, `action`, `validation`, `types`를 함께 다룬다.
- 기능 간 결합은 최소화한다.
- 다른 기능 내부 파일을 직접 참조하기보다 공개된 인터페이스를 사용한다.
- 복잡한 흐름은 feature 단위에서 조합하고, 공통 로직은 server/lib로 내린다.

## 6. 서버 로직과 서비스 계층

- 비즈니스 규칙은 `server/services` 계층에 둔다.
- DB 접근은 `server/repositories` 또는 동등한 계층으로 분리한다.
- 외부 API 호출은 서비스 또는 전용 클라이언트 계층에서만 수행한다.
- 서비스 함수는 입력과 출력이 분명해야 한다.
- 서비스는 UI 프레임워크 세부사항을 몰라야 한다.

## 7. Prisma와 데이터 접근

- Prisma 호출은 페이지나 컴포넌트에서 직접 하지 않는다.
- Prisma 쿼리는 리포지토리 또는 서버 계층에 모은다.
- 하나의 기능이 여러 테이블을 건드리면 트랜잭션 여부를 먼저 검토한다.
- DB 모델명은 도메인 의미가 분명해야 한다.
- nullable 필드는 습관적으로 늘리지 않는다.
- enum, status, type 필드는 문자열 남발보다 구조적으로 관리한다.
- 스키마 변경 시 데이터 마이그레이션 영향까지 같이 본다.

## 8. 타입 규칙

- `any` 사용은 금지에 가깝게 본다.
- 입력, 출력, DB 모델, API 응답 타입을 명확히 구분한다.
- 같은 의미의 타입을 여러 이름으로 중복 선언하지 않는다.
- 추론이 충분한 곳에서는 불필요한 타입 장식을 줄인다.
- 런타임 검증이 필요한 입력은 타입만 믿지 말고 검증 로직을 둔다.

## 9. 검증과 에러 처리

- 사용자 입력은 서버 도착 전후 모두 검증 가능성을 본다.
- 사용자에게 보여줄 에러와 내부 로그용 에러를 분리한다.
- 실패 시 다음 행동이 보이게 에러 메시지를 설계한다.
- catch 블록에서 에러를 삼키지 않는다.
- 재시도 가능한 실패와 즉시 중단해야 하는 실패를 구분한다.
- AI 생성, 외부 API, 발행 작업은 실패 상태를 명시적으로 저장한다.

## 10. 상태 관리

- 서버 상태와 UI 상태를 분리한다.
- 전역 상태는 정말 공통일 때만 쓴다.
- 한 화면에서만 필요한 상태를 전역으로 올리지 않는다.
- 파생 가능한 상태를 중복 저장하지 않는다.
- 비동기 상태는 `idle`, `loading`, `success`, `error`처럼 명확히 다룬다.

## 11. 로깅과 관측성

- 중요한 서버 액션은 시작, 성공, 실패 로그를 남긴다.
- 로그에는 어떤 작업이 실패했는지 추적 가능한 식별자를 넣는다.
- 민감정보는 로그에 남기지 않는다.
- AI 생성, 발행, 이미지 생성, 컨텍스트 분석은 작업 ID 단위로 추적 가능해야 한다.
- 운영 이슈 재현에 필요한 최소 맥락은 로그에서 볼 수 있어야 한다.

## 12. 테스트 기준

- 새 기능은 최소한 핵심 성공 경로를 검증한다.
- 버그를 수정했으면 같은 버그를 막는 검증을 추가한다.
- 서비스 계층은 단위 테스트 대상 우선순위로 본다.
- 여러 계층이 연결되는 기능은 통합 테스트가 필요하다.
- 테스트가 아직 없더라도, 테스트하기 어려운 구조를 새로 만들지 않는다.

## 13. UI 구현 기준

- UI는 예쁘기보다 먼저 사용 흐름이 명확해야 한다.
- 한 화면의 주요 액션은 시각적으로 분명해야 한다.
- 로딩, 빈 상태, 실패 상태를 반드시 설계한다.
- 자동완성, 기본값, 추천값으로 입력 부담을 줄인다.
- 반복 편집이 많은 화면은 클릭 수를 줄이는 방향으로 설계한다.
- 텍스트 기반 기능은 복사, 재생성, 비교, 되돌리기 흐름이 쉬워야 한다.

## 14. AI 기능 구현 기준

- AI 출력은 항상 비결정적이라는 전제로 설계한다.
- 생성 결과는 검수 전 확정 데이터로 다루지 않는다.
- 프롬프트 버전, 입력 맥락, 생성 시각을 추적 가능하게 저장한다.
- 재생성은 원본을 덮어쓰기보다 버전으로 관리한다.
- 브랜드 컨텍스트와 사용자 입력이 충돌하면 사용자 승인 단계를 우선한다.
- 이미지 생성은 비주얼 생성과 텍스트 합성 책임을 분리한다.

## 15. 보안 기준

- 서버 전용 로직과 시크릿은 클라이언트 번들로 보내지 않는다.
- 권한이 필요한 작업은 UI 숨김으로 끝내지 말고 서버에서 다시 확인한다.
- 파일 경로, 외부 URL, 사용자 입력은 신뢰하지 않는다.
- 업로드 파일과 외부 응답은 검증 전 바로 사용하지 않는다.

## 16. 리뷰 체크리스트

- 이 변경은 한 가지 목적만 해결하는가
- 반복되는 코드가 새로 생기지 않았는가
- UI, 서비스, DB 책임이 섞이지 않았는가
- 롤백 가능한가
- 로그와 에러 처리가 충분한가
- 테스트 또는 검증 방법이 남아 있는가
- 문서 업데이트가 필요한 변경인가

## 17. 금지 사항

- 컴포넌트 안에서 직접 Prisma 호출 금지
- 페이지 파일에 장문 비즈니스 로직 작성 금지
- Route Handler에 중복 로직 복붙 금지
- 에러 무시 금지
- 타입 우회용 `any` 남용 금지
- 검수 없는 AI 결과 저장 및 발행 금지
- 테스트 없이 DB 구조 변경 금지
