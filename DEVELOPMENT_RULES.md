# m-master 개발 절대규칙

이 문서는 `m-master`에서 예외 없이 지켜야 하는 개발 기준이다.

## 1. 브랜치와 배포

- 모든 일상 개발은 반드시 `production` 브랜치 기준으로 진행한다.
- `production` 브랜치에는 직접 기능 개발을 하지 않는다.
- 모든 배포는 `production` 푸시 기준으로 `m-master.vercel.app`에 바로 반영된다.
- 운영 반영은 반드시 `production` 브랜치 머지를 통해서만 진행한다.
- `production` 반영은 명시적인 승인과 확인 없이 진행하지 않는다.
- 로컬에서 `vercel --prod`로 직접 운영 배포하지 않는다.

## 2. Pull Request

- `production`으로 가는 변경은 반드시 PR로 올린다.
- PR에는 변경 목적, 영향 범위, 테스트 결과를 반드시 적는다.
- UI 변경 PR에는 화면 캡처 또는 테스트 도메인 링크를 남긴다.
- 데이터 모델 변경 PR에는 마이그레이션 영향과 롤백 방법을 적는다.

## 3. 데이터베이스

- 이 프로젝트의 DB는 `m-master` 전용 Neon DB만 사용한다.
- 다른 프로젝트 DB와 테이블을 공유하거나 섞지 않는다.
- 스키마 변경은 반드시 Prisma 스키마 기준으로 관리한다.
- 운영 데이터에 직접 수작업 수정이 필요하면 먼저 백업 가능성과 영향 범위를 확인한다.
- 대량 수정, 삭제, 구조 변경은 테스트 배포에서 먼저 검증한다.

## 4. 비밀정보와 환경변수

- 토큰, API 키, DB 접속 정보는 코드에 하드코딩하지 않는다.
- 민감정보는 Git에 커밋하지 않는다.
- `.env.local`은 로컬 전용이며 저장소에 올리지 않는다.
- 새 환경변수가 생기면 `.env.example`과 문서에 목적을 기록한다.
- 시크릿 값은 GitHub Actions, Vercel, Neon의 관리 기능으로만 주입한다.

## 5. 코드 변경 원칙

- 작게 변경한다. 큰 기능도 작은 PR로 쪼갠다.
- 한 PR은 한 가지 목적만 해결한다.
- 한 번에 한 가지 이유만 변경한다. 기능 추가, 리팩터링, 버그 수정을 섞지 않는다.
- 기획 변경, UI 변경, 데이터 구조 변경을 한 번에 섞지 않는다.
- 되돌릴 수 있게 만든다. 롤백 경로 없는 변경을 만들지 않는다.
- 근본 원인 수정 없이 임시 우회 코드만 남기지 않는다.
- 명확성은 영리함보다 우선한다. 읽기 쉬운 코드를 선호한다.
- 중복 로직은 그대로 복사하지 말고 공통화 가능성을 먼저 본다.
- 반복되는 UI와 기능은 반드시 컴포넌트 또는 공통 모듈로 개발한다.
- 테스트 가능한 구조를 먼저 만든다. 나중에 테스트 붙이기 어려운 구조를 피한다.
- 경계가 중요하다. UI, 도메인, DB, 외부 API 책임을 섞지 않는다.
- 삭제 가능한 구조를 선호한다. 기능을 덧붙이기만 하는 구조를 피한다.
- 사용하지 않는 코드, 파일, 플래그는 방치하지 않는다.

## 6. UI와 UX

- 모든 UI는 먼저 사용자 편의 기준으로 판단한다.
- 기능 추가보다 사용 흐름 단순화가 우선이다.
- 핵심 액션은 한 화면에서 바로 보이게 둔다.
- 사용자 입력 부담을 줄이는 자동완성, 추천, 기본값을 우선 설계한다.
- 모바일과 데스크탑 모두에서 주요 흐름이 깨지지 않아야 한다.
- 디자인은 현재 정의된 스타일 시스템을 따른다.
  - `Paperlogy`
  - 3색 이내 사용
  - Apple Korea 기반 미니멀 방향 유지

## 7. 콘텐츠와 기획 반영

- 이 플랫폼의 핵심은 `컨텍스트 이해 기반 생성`이다.
- 모든 기능은 작업 폴더 분석, 도메인 분석, 브랜드 컨텍스트 구축이라는 제품 정의를 훼손하면 안 된다.
- 블로그는 원본 자산이고, 인스타그램과 페이스북은 채널별 파생 자산으로 다룬다.
- AI 이미지 생성 기능은 `비주얼 생성 + 플랫폼 텍스트 합성` 원칙을 유지한다.
- 기획 의도와 다른 방향의 편의적 축소는 대장님 승인 없이 하지 않는다.

## 8. 품질 검증

- 새 기능은 최소한 로컬 실행 또는 빌드 검증 후 올린다.
- 배포 전에는 로컬 검증과 필요 시 정렬 브랜치 PR 검증으로 마무리한다.
- 실패한 배포를 무시하고 다음 작업으로 넘어가지 않는다.
- 에러 로그가 있으면 증상만 고치지 말고 원인을 확인한다.
- 관측 가능하게 만든다. 로그, 에러, 상태를 확인할 수 있어야 한다.
- 성능 최적화는 측정 후 진행한다. 감으로 최적화하지 않는다.
- 기본값은 안전하게 둔다. 실수 시 망가지는 방향보다 막히는 방향을 우선한다.

## 9. 문서화

- 구조가 바뀌면 README 또는 관련 문서를 함께 갱신한다.
- 배포 흐름, 환경변수, DB 구조가 바뀌면 문서 수정은 필수다.
- 문서보다 코드가 진실이지만, 운영 규칙은 반드시 문서화한다.
- 사람 기억에 의존하는 운영 절차를 만들지 않는다.
- 구현 전에는 반드시 `wiki/`를 먼저 확인하고, 구현 중 판단은 위키에 기록한다.
- 개발 문서는 원칙적으로 `500줄 미만`으로 유지한다.
- 문서가 500줄을 넘길 정도로 커지면 주제별로 분리한다.

## 10. 고급 개발 원칙

- 운영 데이터는 신성하다. 테스트 없는 DB 변경을 금지한다.
- 자동화되지 않은 반복 작업은 빚이다. 배포, 검사, 검증은 자동화한다.
- 보안은 나중 일이 아니다. 시크릿, 권한, 공개 범위를 초기에 막는다.
- 팀 규칙은 예외 없이 적용한다. 누구 한 명만 다르게 하면 규칙이 무너진다.

## 11. 이 프로젝트 전용 고급 규칙

- 운영과 배포 기준 브랜치는 `production` 하나로 유지한다.
- DB 스키마 변경은 리뷰 없이 금지한다.
- AI 결과물은 검수 레이어 없이 바로 발행하지 않는다.
- 브랜드 컨텍스트 추출 결과는 사용자 승인 전 확정하지 않는다.
- 이미지 생성 결과는 텍스트 정확도 검증 없이 사용하지 않는다.
- 문서 `500줄` 초과는 금지하고, 초과 시 분리한다.
- 직접 배포는 금지하고, GitHub Actions만 배포 주체로 사용한다.
- 운영 브랜치 직접 push는 금지한다.

## 12. 금지 사항

- `production` 직접 푸시 남용 금지
- 운영 DB 임의 수정 금지
- 테스트 없이 운영 반영 금지
- 비밀정보 커밋 금지
- 기존 프로젝트와 자산 혼용 금지
- 대장님 승인 없는 방향 전환 금지

## 13. 기본 작업 순서

1. `production`에서 작업한다.
2. 로컬 검증을 한다.
3. `production`에 푸시한다.
4. `m-master.vercel.app`에서 확인한다.
5. PR로 `production` 승격을 요청한다.
6. 승인 후 `production` 머지로 운영 반영한다.

## 14. 승격 체크리스트

1. `production` 브랜치 HEAD가 배포 대상 최신 커밋인지 확인한다.
2. `git status`가 의도한 변경만 포함하는지 확인한다.
3. `npm run build`를 통과시킨다.
4. `origin/production` 푸시 후 GitHub Actions production 배포 성공을 확인한다.
5. `m-master.vercel.app`에서 실제 사용자 흐름을 확인한다.
7. 저장소 정책에 맞는 방식으로 머지한다.
8. production deploy workflow 성공을 확인한다.
9. `m-master.vercel.app` 응답과 주요 화면을 확인한다.

## 15. 승격 예외 처리

- `production` 직접 push나 force push는 하지 않는다.
- `production` 직접 반영이 충돌나면, 최신 `origin/production` 기준 임시 정렬 브랜치를 만든다.
- 임시 정렬 브랜치에 검증된 변경을 반영한 뒤 `production`으로 새 PR을 만든다.
- PR 머지 후 production deploy가 자동으로 뜨지 않으면 `deploy.yml`을 `production` 기준으로 수동 실행한다.
