# m-master 검증 규칙

이 문서는 `m-master`에서 코드, 데이터, UI, AI 결과물, 배포를 검증하는 기준이다.

## 1. 기본 원칙

- 검증 없는 구현은 완료로 보지 않는다.
- `작동한다`와 `검증됐다`를 같은 뜻으로 쓰지 않는다.
- 검증은 로컬, 승격 전, 운영 반영 전 단계로 나눠 본다.
- 실패한 검증은 우회하지 않고 원인을 확인한다.

## 2. 검증 단계

- `로컬 검증`
  - 빌드 가능 여부
  - 기본 실행 여부
  - 변경 범위 핵심 흐름 확인
- `승격 검증`
  - `test` 브랜치에 운영 반영 대상 커밋이 모두 포함됐는지 확인
  - 외부 연동, 상태 변화, 예외 흐름을 로컬 또는 승인된 점검 절차로 확인
- `production 전 검증`
  - 머지 대상 diff 검토
  - 영향 범위 확인
  - 롤백 가능성 확인

## 3. PR 검증 규칙

- 모든 PR에는 변경 목적이 분명해야 한다.
- 모든 PR에는 검증 방법이 적혀 있어야 한다.
- 모든 PR에는 관련 위키 문서 또는 새로 기록한 위키 문서가 연결돼야 한다.
- UI 변경은 화면 캡처 또는 검증 근거가 있어야 한다.
- DB 변경은 스키마 영향과 데이터 영향이 설명돼야 한다.
- 운영 영향이 있는 변경은 롤백 방법을 적어야 한다.

## 4. 코드 검증 규칙

- 변경 범위에 불필요한 코드가 섞이지 않았는지 본다.
- 같은 로직이 중복 추가되지 않았는지 본다.
- 책임 분리가 깨지지 않았는지 본다.
- 예외 처리와 에러 메시지가 남아 있는지 본다.
- 로그가 필요한 지점에 빠지지 않았는지 본다.
- 타입 우회가 들어가지 않았는지 본다.

## 5. UI 검증 규칙

- 주요 액션이 한눈에 보이는지 확인한다.
- 로딩, 빈 상태, 실패 상태가 있는지 확인한다.
- 모바일과 데스크탑 주요 흐름이 유지되는지 확인한다.
- 입력 부담이 과도하지 않은지 확인한다.
- 반복 작업이 많은 화면에서 클릭 수가 과하지 않은지 확인한다.
- 문구, 버튼, 안내 텍스트가 사용자 기준으로 명확한지 확인한다.

## 6. API와 서버 검증 규칙

- 정상 입력에서 기대 응답이 나오는지 확인한다.
- 잘못된 입력에서 안전하게 실패하는지 확인한다.
- 인증 또는 권한이 필요한 작업이 서버에서 막히는지 확인한다.
- 외부 API 실패 시 상태 저장과 에러 처리 흐름이 있는지 확인한다.
- 서버 액션이 추적 가능한 로그를 남기는지 확인한다.

## 7. 데이터베이스 검증 규칙

- 스키마 변경 전 영향 테이블과 컬럼을 확인한다.
- nullable, default, unique 제약이 의도와 맞는지 확인한다.
- 대량 변경은 테스트 환경에서 먼저 검증한다.
- 데이터 삭제나 구조 변경은 롤백 가능성을 먼저 본다.
- Prisma 변경 후 쿼리 흐름이 깨지지 않았는지 확인한다.

## 8. AI 결과 검증 규칙

- AI 결과물은 검수 전 확정본으로 취급하지 않는다.
- 브랜드 톤과 금지 표현 위반 여부를 확인한다.
- 사실성, 과장 표현, 위험 표현 여부를 확인한다.
- 블로그, 인스타그램, 페이스북 채널 문법에 맞는지 확인한다.
- 재생성 시 이전 버전과 비교 가능해야 한다.

## 9. 이미지 검증 규칙

- 이미지 생성 결과는 바로 발행하지 않는다.
- 텍스트가 정확한지 별도 확인한다.
- 브랜드 컬러, 톤, 금지 스타일 위반 여부를 확인한다.
- 채널 규격과 해상도가 맞는지 확인한다.
- 중요한 정보가 잘리거나 가려지지 않았는지 확인한다.

## 10. 배포 검증 규칙

- `test` 브랜치 검증 없이 `production` 승격 금지
- GitHub Actions 성공 여부 확인
- 운영 반영 대상 diff 확인
- 환경변수 누락 여부 확인
- 운영 반영 전 변경 범위와 롤백 경로 재확인

## 11. 보안 검증 규칙

- 시크릿이 코드나 로그에 노출되지 않았는지 확인한다.
- 서버 전용 값이 클라이언트로 노출되지 않았는지 확인한다.
- 업로드 파일, 외부 URL, 사용자 입력 검증이 있는지 확인한다.
- 권한 검증이 UI가 아니라 서버에서도 수행되는지 확인한다.

## 12. 최소 체크리스트

- 빌드 성공
- 핵심 기능 수동 확인
- 에러 흐름 확인
- 로그 확인
- 문서 업데이트 여부 확인
- 승격 대상 diff 확인
- 운영 영향 확인

## 13. 금지 사항

- 테스트 없이 운영 반영 금지
- 실패한 검증 무시 금지
- AI 결과 무검수 발행 금지
- DB 영향 확인 없는 스키마 변경 금지
- 검증 결과 기록 없는 대형 변경 금지
